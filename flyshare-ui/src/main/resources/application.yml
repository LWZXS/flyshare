server:
  port: 8222

spring:
  application:
    name: flyshare-ui
    
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
       defaultZone: http://${eureka.instance.hostname}:8000/eureka/
 
   