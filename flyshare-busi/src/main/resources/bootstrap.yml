spring:
  cloud:
    config:
      discovery:
        enabled: true #开启通过服务来访问Config Server的功能
        service-id: flyshare-config
      name: flyshare-config  
      profile: online
      label: master
      
eureka:
  client:
    service-url:
       defaultZone: http://root:123456@127.0.0.1:8000/eureka/