spring:
  application:
    name: flyshare-config # 对应config-server所获取的配置文件的{application}
  cloud:
    config:
      uri: http://localhost:8555/
      profile: online # 指定profile，对应config-server所获取的配置文件中的{profile}
      label: master # 指定git仓库的分支，对应config-server所获取的配置文件的{label}